// Stub Firebase exports for PostgreSQL migration
// This prevents Firebase initialization errors while maintaining import compatibility

export const auth = null;
export const db = null;
export const storage = null;
export const analytics = null;

export default null;